 #set( $dollar = '$' )
server:
  port: ${dollar}{PORT:${eureka-port}}
spring:
  application:
    name: eureka
  cloud:
    config:
      uri: ${dollar}{URL:http://${config-server-host}:${config-server-port}}
      fail-fast: true